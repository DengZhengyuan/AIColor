# AIColor Experiment Registry

This folder tracks small experiment manifests and run instructions.

Large artifacts are intentionally kept outside Git:

- `data/raw/`
- `data/processed/`
- `results/`
- `logs/`
- model checkpoints and weights

Use dated experiment namespaces for all non-Git data and results.
